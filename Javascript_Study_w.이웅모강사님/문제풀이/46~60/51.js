/* 문자열을 정수로 변환하기
숫자로만 이루어진 문자열 nStr이 주어질 때, nStr을 정수로 변환하여 return
*/

const solution = (nStr) => {
  return +nStr;
};

console.log(solution("10")); // 10
console.log(solution("8542")); // 8542

/* == 라이브 도중 추가 == */
